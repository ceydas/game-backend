package com.dreamgames.backendengineeringcasestudy.tournament_session.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class TournamentSessionController {

    /*
    @PutMapping("/tournament/{tid}/claim-reward/{id}")
    public ResponseEntity<UserDto> claimReward(@PathVariable Long tid, @PathVariable Long id){
        //todo implement logic for claiming the reward:
           @transactional
           get group rank (using group id and score)
           decide reward
           update tournament session (update claimReward = true, latestTournamentId)
           save user

    }

     */
}
